This folder contains all static resources used by HTML pages(from folder `templates`).

This includes CSS files specifying HTML themes.

Also includes Javascript files specifying backend GUI functions.